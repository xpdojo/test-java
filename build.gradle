plugins {
    id "java"
}

allprojects {
    repositories {
        mavenCentral()
    }

    group = 'com.markruler'
}

subprojects {
    apply plugin: "java"

    sourceCompatibility = JavaVersion.VERSION_17
    targetCompatibility = JavaVersion.VERSION_17
}
